<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.simple.studio">

    <application
        android:name="com.simple.studio.base.ConfigApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat"
        >
        <activity android:name="com.simple.studio.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 全局变量配置 -->
<!--        <meta-data
            android:name="env_key"
            android:value="test" />
        <meta-data
            android:name="env_key0"
            android:value="${env_val0}" />
        <meta-data
            android:name="env_key1"
            android:value="${env_val1}" />-->

        <activity android:name="com.simple.studio.manifest.ManifestPropActivity"></activity>
    </application>

</manifest>